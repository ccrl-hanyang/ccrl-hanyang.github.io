# CCRL 홈페이지 편집 방법

준비할 게 없습니다. GitHub 계정으로 로그인만 되어 있으면 됩니다.
권한 없는 사람에게는 편집 버튼이 아예 보이지 않으므로, 이것이 사실상의 로그인 기능입니다.

---

## 편집 화면 여는 방법 2가지

### 방법 1 — 파일 하나만 고칠 때

1. 저장소에서 고칠 파일을 클릭 (예: `students.html`)
2. 오른쪽 위 **연필 아이콘** (Edit this file)
3. 내용 수정
4. 오른쪽 위 **Commit changes...** → 초록 버튼 **Commit changes**
5. 1~2분 뒤 사이트에 반영. 안 바뀌어 보이면 `Ctrl + Shift + R`

### 방법 2 — 여러 파일을 한 번에 (권장)

저장소 첫 화면에서 키보드 **`.` (마침표)** 를 누르세요.
브라우저에서 VS Code가 열립니다. 설치 필요 없습니다.

- 왼쪽에서 파일 선택, `Ctrl + F` 로 검색
- 다 고쳤으면 왼쪽 **Source Control** (가지 아이콘) → 메시지 입력 → **Commit & Push**

여러 곳을 동시에 고칠 때는 이쪽이 훨씬 빠릅니다.

---

## 뭘 어디서 고치나

| 하고 싶은 일 | 파일 |
|---|---|
| 논문 추가 | `pub-intl-journal.html` 등 해당 파일 |
| 특허 추가 | `patent.html` |
| 과제 추가 | `project.html` |
| 구성원 추가·삭제 | `students.html` |
| 졸업생 추가 | `alumni.html` |
| 교수님 정보 | `professor.html` |
| 공지·News | `notice.html` `news.html` |
| 연구 소개 글 | `introduction.html` `research.html` |
| 주소·전화 | 각 파일 맨 아래 `<footer>` + `location.html` |
| 색·글꼴·여백 | `styles.css` (전 페이지 공통) |
| 팝업 내용 | `index.html` 안의 `<div class="pop"` |

---

## 복사해서 쓰는 양식

### 논문 추가

`pub-intl-journal.html` 을 열고 `<tbody>` 를 찾으세요. 그 **바로 다음 줄**에 붙여넣습니다
(위쪽이 최신입니다).

```html
        <tr><td class="n">71</td><td>저자 1, 저자 2, and Dong-Joon Shin, "논문 제목," 학술지 이름, vol. 00, no. 0, pp. 000-000, Mon. 2026.</td></tr>
```

**두 곳을 같이 고쳐야 합니다.**

1. `<td class="n">71</td>` — 기존 맨 위 번호 + 1
2. 표 위의 `전체 70` → `전체 71`

따옴표는 반드시 `&quot;` 로 쓰세요. 그냥 `"` 를 쓰면 표가 깨집니다.

### 구성원 추가

`students.html` 에서 해당 섹션(`Ph. D. Candidates` 등)을 찾아 `<div class="pc">` 블록을
하나 복사해 붙이고 내용만 바꾸세요.

```html
        <div class="pc">
          <img class="pc__ph" src="uploads/2026/09/사진파일명.jpg" alt="홍길동" loading="lazy" onerror="this.classList.add('miss');this.removeAttribute('src')">
          <h3>홍 길 동</h3>
          <dl>
            <dt>Research Interests</dt><dd><ul><li>연구 주제 1</li><li>연구 주제 2</li></ul></dd>
          </dl>
        </div>
```

이름은 글자 사이에 **공백을 넣습니다** (`홍 길 동`). 원본 사이트 방식입니다.

사진은 먼저 `uploads/연도/월/` 폴더에 올리고 그 경로를 적으세요.
사진이 없으면 `src` 값을 아무거나 둬도 사선 무늬 자리표시자가 나옵니다.

**학생 이메일과 전화번호는 넣지 않는 것이 현재 방침입니다.**

### 졸업생 추가 (구성원이 졸업했을 때)

1. `students.html` 에서 그 사람의 `<div class="pc">` 블록을 **잘라내기**
2. `alumni.html` 의 `Ph. D.` 또는 `M. S.` 섹션 맨 앞에 **붙여넣기**
3. `<dl>...</dl>` 부분을 이력과 논문으로 바꾸기

```html
        <div class="pc">
          <img class="pc__ph" src="uploads/2019/03/사진.png" alt="홍길동" loading="lazy" onerror="this.classList.add('miss');this.removeAttribute('src')">
          <h3>홍 길 동</h3>
          <p class="ac__h">한양대학교 융합전자공학과 박사 (~2026.8)</p>
          <p class="ac__h">삼성전자 (2026.9~)</p>
          <p class="ac__t">졸업논문 제목을 여기에</p>
        </div>
```

`ac__h` = 이력 (여러 줄 가능), `ac__t` = 졸업논문 (점선 위에 표시됨)

### 과제 추가

`project.html` 의 `Ongoing` 표에서 `<tbody>` 다음 줄에:

```html
        <tr><td class="n">3</td><td class="pj">연구과제 이름</td><td class="pd">2026.09.01 ~ 2029.08.31</td><td class="og">한국연구재단</td></tr>
```

`전체 2` 숫자도 같이 고치세요.

**과제가 끝나면** 그 `<tr>` 을 잘라서 `Completed` 표(같은 파일 아래쪽) 맨 앞으로 옮기고,
양쪽 번호와 `전체 N` 을 맞추세요.

### 공지 추가

`notice.html` 의 `<tbody>` 다음 줄에:

```html
        <tr><td class="n">10</td><td class="y">2026-09-01</td><td>[공지] 제목<span class="note">본문 내용</span></td></tr>
```

`<span class="note">` 부분은 생략해도 됩니다.

---

## 안전하게 고치는 요령

**커밋 전에 미리 보기**
편집 화면 위쪽 **Preview** 탭을 누르면 HTML이 어떻게 보일지 대략 확인됩니다.

**잘못 고쳤을 때 되돌리기**
파일 화면 오른쪽 위 **History** → 되돌리고 싶은 커밋 클릭 → 오른쪽 **···** → **Revert**.
Git이 모든 이전 버전을 보관하므로 사이트를 망칠 수 없습니다. 겁내지 말고 고치세요.

**태그 짝을 맞추세요**
`<td>` 를 열었으면 `</td>` 로 닫아야 합니다. 표가 이상하게 보이면 십중팔구 닫는 태그 누락입니다.

**한글은 그냥 쓰면 됩니다**
파일이 UTF-8이라 한글이 깨지지 않습니다. 단 `"` `<` `>` `&` 는
각각 `&quot;` `&lt;` `&gt;` `&amp;` 로 써야 안전합니다.

---

## 후임자에게 넘길 때

조직 **People** → **Invite member** → 계정 초대 → 역할 **Owner**

Owner를 최소 2명 두세요. 한 명뿐이면 그 사람이 2단계 인증 기기를 잃어버리는 순간
조직 전체가 잠깁니다.

이 문서와 `generator/` 폴더를 함께 넘기면 됩니다.

---

## 손으로 고치기 번거로워지면

논문이 수백 편이라 한 편 추가할 때마다 번호와 `전체 N` 을 맞추는 게 귀찮습니다.
`generator/` 안의 `build.py` 가 DB 덤프에서 전체 페이지를 다시 만드는 스크립트인데,
파이썬을 쓸 수 있는 사람이 있으면 그쪽이 더 편합니다.

Google 스프레드시트에 논문 목록을 두고 사이트가 읽어가게 만드는 방법도 있습니다.
그렇게 하면 시트에 한 줄 추가하는 것만으로 끝나고 번호도 자동으로 매겨집니다.

---

# 관리자 페이지 (admin.html)

버튼으로 항목을 추가·삭제할 수 있는 화면입니다. 번호와 `전체 N` 이 자동으로 다시 계산되므로
손으로 맞출 필요가 없습니다.

## 처음 한 번만 — 토큰 만들기

토큰이 암호 역할을 합니다. 페이지 소스에 들어가지 않고 브라우저에만 저장됩니다.

1. GitHub → 프로필 → **Settings** → 맨 아래 **Developer settings**
2. **Personal access tokens** → **Fine-grained tokens** → **Generate new token**
3. 설정
   - Token name: `ccrl-admin` 등
   - Expiration: 1년
   - **Repository access → Only select repositories** → 이 저장소만 선택
   - **Permissions → Repository permissions → Contents → Read and write**
4. Generate token → `github_pat_...` 문자열 복사

이 문자열을 잃어버리면 다시 볼 수 없으니, 비밀번호 관리 앱이나 안전한 곳에 보관하세요.

## 사용법

1. `https://<사이트주소>/admin.html` 접속
2. 조직명, 저장소 이름, 토큰 입력 → **연결 확인**
3. 추가할 곳 선택 → 내용 입력 → **추가하기**
4. 1~2분 뒤 사이트 반영

`목록 불러오기` 를 누르면 최근 20건이 나오고 각 항목을 삭제할 수 있습니다.

## 관리자 페이지로 되는 것

논문 4종 · 특허 3종 · 과제(진행/완료) · Notice · News — 총 11곳

구성원과 졸업생은 사진이 필요해서 관리자 페이지에 넣지 않았습니다.
`students.html` / `alumni.html` 을 직접 수정하세요 (위쪽 양식 참고).

## 보안에 대해

- `admin.html` 은 공개돼 있지만 **토큰이 없으면 아무것도 못 합니다.** 검색엔진에는 노출되지 않도록 설정했습니다
- 토큰을 남에게 주지 마세요. 유출되면 GitHub에서 **Revoke** 하면 즉시 무효화됩니다
- 토큰 권한을 이 저장소 하나로 제한했으므로, 최악의 경우에도 다른 저장소는 안전합니다
- 잘못 올렸을 때는 저장소 **History → 해당 커밋 → ··· → Revert** 로 되돌립니다

---

# 구성원 관리 (관리자 페이지 3~4번)

## 순서 규칙

**새로 들어온 사람은 아래에, 선배가 위에** 표시됩니다.
졸업생 목록도 같은 규칙이라 최근 졸업자가 맨 아래입니다.
관리자 페이지가 이 규칙대로 자동 배치합니다.

## 구성원 추가

관리자 페이지 **3. 구성원 관리**

1. 그룹 선택 (Post-Doc. / Ph. D. Candidates / Master Candidates / Undergraduate Students)
2. 이름은 **한글로** 입력 (`홍길동`). 화면에는 `홍 길 동` 처럼 자간이 벌어져 표시됩니다
3. 사진 파일 선택 — `uploads/연도/월/` 에 자동 업로드됩니다. 없어도 됩니다
4. 연구 주제는 한 줄에 하나씩
5. **구성원 추가**

사람이 없는 그룹은 화면에서 자동으로 숨겨집니다.

## 과정 변경 (석사 → 박사 등)

관리자 페이지에는 별도 버튼이 없습니다. 두 가지 방법이 있습니다.

- 기존 카드를 **삭제**한 뒤 새 그룹으로 **다시 추가** (사진을 다시 올려야 함)
- 또는 `students.html` 에서 `<div class="pc">` 블록을 잘라 다른 그룹의
  `data-group="..."` 안쪽 맨 아래로 옮기기 (사진 유지)

## 졸업 처리

관리자 페이지 **4. 졸업 처리**

1. **구성원 목록** 을 먼저 눌러 대상을 불러옵니다
2. 졸업한 사람 선택
3. 학위 (Ph. D. / M. S.)
4. 이력을 한 줄에 하나씩. 예)
   ```
   한양대학교 융합전자공학과 박사 (~2027.2)
   삼성전자 (2027.3~)
   ```
5. 졸업논문 제목 (선택)
6. **졸업 처리**

Students 에서 빠지고 Alumni 맨 아래에 추가됩니다. 사진은 그대로 따라갑니다.
커밋이 두 번 일어나므로(students, alumni) 중간에 실패하면 메시지가 안내하는 대로
저장소 History 를 확인하세요.

---

# 지도 좌표 고치기

`location.html` 의 지도 핀 위치가 정확하지 않으면 좌표만 바꾸면 됩니다.

## 정확한 좌표 얻기 (30초)

1. Google 지도에서 구자겸 기계관 위치를 화면에 띄웁니다
2. 건물 위에서 **마우스 오른쪽 클릭**
3. 맨 위에 나오는 숫자 두 개 (`37.5581, 127.0473` 형태)를 클릭하면 복사됩니다

## 고칠 곳

`location.html` 을 열고 `maps?q=` 로 시작하는 줄에서 숫자 두 개를 바꾸세요.
같은 파일 아래쪽 `Google Maps` 링크의 `query=` 뒤 숫자도 같이 바꿉니다.

생성기를 쓰는 경우에는 `generator/build.py` 의 이 줄 하나만 고치고 다시 돌리면 됩니다.

```python
MAP_LAT, MAP_LNG = 37.5581, 127.0473
```

## 참고

건물이 2026년 6월에 완공되어 Google 지도에 아직 이름이 없을 수 있습니다.
그래서 이름 검색 대신 좌표로 핀을 찍고, 네이버 지도·카카오맵 링크를 함께 넣었습니다.
국내 지도 서비스는 신축 건물 반영이 더 빠릅니다.
